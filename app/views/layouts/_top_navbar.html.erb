<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">SS</a>
      </div>
      <div class="navbar-right">
        <% if user_signed_in? %>
          <p class="navbar-text">
            <span class="profile_email"><%= current_user.email %></span>
            <%= link_to '', destroy_user_session_path, :method => :delete, class: 'glyphicon glyphicon-log-out navbar-link' %>
          </p>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "btn btn-default navbar-btn" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-success navbar-btn" %>
        <% end %>
      </div>
  </div>
</nav>